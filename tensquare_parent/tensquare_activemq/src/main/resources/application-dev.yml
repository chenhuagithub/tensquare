server:
  port: 8006

spring:
  application:
    name: tensquare_activemq

  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false


myqueue: boot-activemq-queue
